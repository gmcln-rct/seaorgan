
export const setUpSounds = (tideObj) => {
    let notesList = []
    let allNotes = ["A1", "A2", "A3", "A4", "b1", "b2", "b3", "b4", "b5", "C2", "C3", "C4", "C5", "D2", "D3", "D4", "E2", "E3", "E4", "F2", "F3", "F4", "G2", "G3", "G4"];
    let allNotesLength = allNotes.length;

    let noteIdx;
    let newNote;

    for (let i = 0; i < tideObj.length; i++) {
        noteEle = tideObj[i]
        newNote = allNote[noteIdx]
        noteList.push(tideObj[0])
    }



    noteList = allNotes;

    return notesList;
};

